{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/migrate_v1.py\" 2>/dev/null || true"
          },
          {
            "type": "command",
            "command": "_D=$(cat); PYTHONPATH=\"${CLAUDE_PLUGIN_ROOT}/hooks\" python3 -m il_telemetry.scan 2>/dev/null; printf '%s' \"$_D\" | PYTHONPATH=\"${CLAUDE_PLUGIN_ROOT}/hooks\" python3 -m il_telemetry.flush 2>/dev/null || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "PYTHONPATH=\"${CLAUDE_PLUGIN_ROOT}/hooks\" python3 -m il_telemetry.stop 2>/dev/null || true"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "_D=$(cat); PYTHONPATH=\"${CLAUDE_PLUGIN_ROOT}/hooks\" python3 -m il_telemetry.scan 2>/dev/null; printf '%s' \"$_D\" | PYTHONPATH=\"${CLAUDE_PLUGIN_ROOT}/hooks\" python3 -m il_telemetry.flush 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}